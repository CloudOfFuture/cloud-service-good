server:
  port: 7010
spring:
  application:
    name: cloud-service-good
  cloud:
    config:
      uri: http://192.168.0.119:8762/
      label: master
      profile: dev
    bus:
      trace:
        enabled: true

